[
  (body_expression "}" @end)
  (record_expr "}" @end)
  (record_type "}" @end)
  (list_expr "]" @end)
  (list_pattern "]" @end)
  (tags_type "]" @end)
  (tuple_expr ")" @end)
  (tuple_type ")" @end)
  (parenthesized_expr ")" @end)
  (parenthesized_type ")" @end)
  (nominal_methods "}" @end)
] @indent

[
  (value_declaration)
  (var_declaration)
  (anon_fun_expr)
  (expect)
  (if_expr)
  (then)
  (else)
  (else_if)
  (match_expr)
  (match_branch)
  (for_expr)
  (while_expr)
] @indent
